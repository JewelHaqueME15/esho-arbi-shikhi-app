# Keep the source PDF out of the public repo/deployment
*.pdf
.claude/settings.local.json

# Backend
node_modules/
.env
.env.local
.vercel

# OCR page images (large, regenerable from the PDF)
book-scan/
